package vectorregnum.core;

public enum WildMagicCategory {
    INTERNAL_MANA_DETONATION,
    UNSTRUCTURED_ELEMENT_BURST,
    VIOLENT_MISCAST,
    /** Bounded attention backlash; never grants arbitrary camera control. */
    COERCIVE_ATTENTION
}

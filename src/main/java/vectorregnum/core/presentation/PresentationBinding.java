package vectorregnum.core.presentation;

/** Runtime-resolved geometry source for a cue. */
public enum PresentationBinding {
    CAST_ORIGIN,
    DIRECTION,
    TARGET,
    TARGET_SET,
    PATH,
    IMPACT_POINT,
    AFFECTED_AREA,
    AUTHORED_CIRCLE,
    SCREEN
}

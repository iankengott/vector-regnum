package vectorregnum.core.presentation;

/** Curated renderer families; authored spells select parameters, never executable shader code. */
public enum PresentationCueKind {
    PARTICLES,
    BEAM,
    RIBBON,
    TRAIL,
    RUNES,
    SURFACE,
    VOLUME,
    LIGHT,
    DARKNESS,
    FOG,
    AIR,
    MATERIAL_RESPONSE,
    SPATIAL_SOUND,
    CAMERA,
    SCREEN,
    AFTERMATH
}
